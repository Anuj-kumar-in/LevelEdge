export default function Tooltip({ text, children }: { text: string; children: React.ReactNode }) {
  // Simple stub, replace with real tooltip logic as needed
  return <span title={text}>{children}</span>;
}
